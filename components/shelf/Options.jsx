import { StyledButton } from './styles/Options.js'

const Options = () => (
  <>
    <StyledButton>Editar/añadir reseña</StyledButton>
    <StyledButton warning>Donar libro</StyledButton>
  </>
)

export default Options
